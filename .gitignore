# ── Build output ──
build/
*/build/
.gradle/
.kotlin/

# ── Machine-local, never commit ──
# local.properties carries this machine's Android SDK path.
local.properties

# ── JVM crash dumps (the Gradle daemon leaves these when it runs out of memory) ──
hs_err_pid*.log
replay_pid*.log

# ── IDE ──
.idea/
*.iml
.DS_Store
Thumbs.db

# ── Keys / secrets (M2 device signing, M5 FCM) ──
*.jks
*.keystore
google-services.json
fcm-service-account*.json
